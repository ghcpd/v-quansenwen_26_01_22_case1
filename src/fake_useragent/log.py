"""Logging configuration for the fake_useragent library.

This module provides a configured logger instance used throughout the library
for warning and error messages.
"""

import logging

logger = logging.getLogger(__package__)
